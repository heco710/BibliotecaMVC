@model Autor

@{
    ViewData["Title"] = "Agregar autor";
}

<main class="module-page">
    <div class="container-xl">
        <div class="entity-form-shell">
            <a class="back-link" asp-action="Index">← Volver a autores</a>
            <header class="entity-form-header">
                <span class="module-kicker">Nuevo registro</span>
                <h1>Agregar autor</h1>
                <p>Completa los datos para incorporarlo al catálogo.</p>
            </header>
            <form asp-action="Create" method="post" class="entity-form-card">
                <partial name="_Form" model="Model" />
                <div class="form-actions">
                    <a class="btn btn-outline-secondary" asp-action="Index">Cancelar</a>
                    <button class="btn btn-primary" type="submit">Guardar autor</button>
                </div>
            </form>
        </div>
    </div>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
